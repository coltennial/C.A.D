<h1>Find a Doc </h1>

<%= form_with(model: @doctor, local: true, class: "ui form") do |f| %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, required: true, placeholder: "Doc Name "%>
  </div>
  
  <%= f.submit nil, class: "ui green button" %>

<% end %>
